[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Friendly URL that maps to the TLE proxy function. Query string is forwarded.
[[redirects]]
  from = "/api/tle"
  to = "/.netlify/functions/tle"
  status = 200
